//index, show, store, update, destroy
//Métodos de um controller MVC

const Computer = require('../models/Computers');

module.exports = {
    async store(req, res) {
        //store and destroy
        const { aluno, matricula } = req.body;
        
    }
}